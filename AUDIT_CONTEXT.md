# Repository Audit Context — 18 August 2026

The active local rebuild at `/home/ubuntu/antigravity-pharma-google` has no GitHub remote; its `origin` points to the managed project artifact remote. The intended GitHub destination identified from the authenticated account is `https://github.com/balajirajput96/antigravity-pharma-dashboard`, a private repository on branch `main`.

The checked-out remote head is `d6ec17c` (`ci: verify type checks and unit tests`). The latest visible `Verify application` GitHub Actions run completed successfully, while a Dependabot update run was queued. No open pull requests were listed during the read-only audit.

The remote repository is a broader full-stack workspace with automated audit artifacts, tests, a Firebase migration directory, and Google AI Studio deployment research. It is structurally different from the static local rebuild, so direct history rewriting or blind replacement is unsafe. Compare and integrate the static rebuild deliberately using a feature branch or a reviewed content merge.
